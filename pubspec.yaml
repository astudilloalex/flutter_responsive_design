name: responsive_design
description: Create responsive applications using different widgets that adapt to different screen densities.
version: 0.0.2
repository: https://github.com/astudilloalex/flutter_responsive_design

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: